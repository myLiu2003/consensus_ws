# Change this one value to switch the runtime workspace.
# Example:
#   MAP_CONSENSUS_WS="$HOME/consensus_lcgo_ws"
#   MAP_CONSENSUS_WS="$HOME/consensus_ws"
MAP_CONSENSUS_WS="$HOME/consensus_lcgo_ws"

# Override only if you keep PX4 somewhere else.
PX4_DIR="$HOME/PX4-Autopilot"
